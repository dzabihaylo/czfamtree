'use client';

/**
 * PersonNode — STUB for Task 1 of Plan 02-02. Task 4 implements the full
 * 180x76 card (avatar + gender stripe + name + years + is-me ribbon +
 * selection + drag start). This stub ensures PanZoomWrapper.tsx compiles when
 * `npx next build` runs after Task 1.
 */
type Props = {
  personId: string;
  treeId: string;
};

export default function PersonNode(_props: Props) {
  return null;
}
