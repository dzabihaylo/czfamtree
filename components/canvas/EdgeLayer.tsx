'use client';

/**
 * EdgeLayer — STUB for Task 1 of Plan 02-02. Task 2 implements the full
 * single-SVG edge overlay. This stub ensures PanZoomWrapper.tsx compiles when
 * `npx next build` runs after Task 1.
 */
export default function EdgeLayer() {
  return null;
}
