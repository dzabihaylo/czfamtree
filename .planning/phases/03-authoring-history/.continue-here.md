---
context: phase
phase: 03-authoring-history
plan: 0
total_plans: 4
total_tasks: 13
status: planned_ready_to_execute
last_updated: 2026-05-08T12:23:09.328Z
---

<current_state>
Phase 3 is **planned and ready to execute**. The orchestrator just completed `/gsd-plan-phase 3`:

- Researcher (`gsd-phase-researcher`) ran, wrote `03-RESEARCH.md`, committed as `ff64c73`.
- Pattern-mapper (`gsd-pattern-mapper`) ran, wrote `03-PATTERNS.md`.
- Planner (`gsd-planner`) ran, wrote 4 PLAN.md files.
- Plan-checker (`gsd-plan-checker`) iteration 1 returned **2 BLOCKER + 2 WARNING + 1 INFO**.
- Reviser (`gsd-planner` revision mode) fixed all 4 actionable findings.
- Plan-checker iteration 2 returned **VERIFICATION PASSED**.
- Plans + revised RESEARCH + STATE + ROADMAP committed as `f82aa76`.

**Working tree:** clean (after final commit). 0/4 plans executed. Next command is `/clear` then `/gsd-execute-phase 3`.
</current_state>

<completed_work>

This session (2026-05-07 → 2026-05-08):
- **Research:** Verified zundo 2.3.0 API locally (`pause()` / `resume()` / `pastStates` / `futureStates` confirmed via `node_modules/zundo/dist/index.d.ts`). Confirmed Phase 3 needs **zero new npm packages**. 7 assumptions logged + 4 secondary open questions answered.
- **Pattern mapping:** 22 files classified — 18 in-repo analogs, 4 net-new (Modal, Playwright spec, RPC migration, useTreeKeyboard hook).
- **Planning:** 4 plans across 4 waves with strict sequential dependencies.
- **Revision (iteration 1):** Fixed BLOCKER 1 (Plan 03 depends_on now includes `"03-02"` — wave bumped 2→3); Plan 04 wave bumped 3→4 to sync. Fixed BLOCKER 2 (RESEARCH Open Questions section now `(RESOLVED)` with inline RESOLVED markers Q1..Q5; Q4 implemented as Modal small-viewport clamp). Addressed WARNING 2 (D-03 remove-person undo): TODO marker in source + must_haves.truths note + Plan 04 'Known Debt → Phase 5' SUMMARY entry. WARNING 1 (Plan 04 borderline scope) accepted as-is.
- **Verification:** Iteration 2 passed clean — all 20 phase requirement IDs covered, all 37 CONTEXT.md decisions covered, all 4 plans have `<threat_model>` blocks, every task has `<read_first>` + `<acceptance_criteria>`.
</completed_work>

<remaining_work>

| Wave | Plan | Tasks | Files | Depends on |
|------|------|-------|-------|------------|
| 1 | `03-01-PLAN.md` | 3 | 7 | (none) |
| 2 | `03-02-PLAN.md` | 3 | 7 | 03-01 |
| 3 | `03-03-PLAN.md` | 3 | 7 | 03-01, 03-02 |
| 4 | `03-04-PLAN.md` | 4 | 10 | 03-01, 03-02, 03-03 |

**Plan 1 (wave 1)** — `lib/graph/placement.ts`, `lib/graph/relations.ts` (+ vitest), `app/actions/people.ts` (`addPerson`), `supabase/migrations/20260507180701_add_person_with_relation.sql` (SECURITY DEFINER RPC), `components/ui/Modal.tsx`. **Includes mandatory `npx supabase db push` step inside Task 2** before Plan 02 can call the RPC.

**Plan 2 (wave 2)** — `RadialMenu.tsx` (NEW), `tree-store.ts` (radialOpenFor / autoFocusNameOnNextMount / addOptimisticPerson slice), `PersonNode.tsx` (`+` button wiring), `PanZoomWrapper.tsx` (RadialMenu mount), `SidePanel.tsx` (auto-focus on mount), `fields/FieldInput.tsx`, `TreeCanvas.tsx`.

**Plan 3 (wave 3)** — `tree-store.ts` (temporal config + toasts slice — DIFFERENT region from Plan 2), `useSaveQueue.ts` (+ `enqueueInverse`), `useSaveErrorToast.ts` (NEW), `PanZoomWrapper.tsx` (drag pause/resume bracket — DIFFERENT region from Plan 2), `Toolbar.tsx` (NEW — 8 buttons), `ToastHost.tsx` (NEW), `TreeCanvas.tsx`.

**Plan 4 (wave 4)** — `useTreeKeyboard.ts` (NEW — scope-gated ⌘Z/⌘⇧Z/⌘Y/⌘K/⌘F), `SearchPalette.tsx` (NEW — Cmd+K/Cmd+F + Modal small-viewport clamp), `SidePanel.tsx` (inline-undo Delete refactor — replace `window.confirm()`), `RadialMenu.tsx` (toast emit), `TreeCanvas.tsx` (canvas section + tabIndex), `tree-store.ts` (searchOpen/searchQuery), `e2e/phase-3-demo-path.spec.ts` (NEW Playwright E2E — the demo-path gate from D-01).
</remaining_work>

<decisions_made>

- **`addPerson` ships as a SECURITY DEFINER RPC** (single atomic transaction for symmetric relations), not as a two-write Server Action. Migration is committed as part of Plan 01. Resolves RESEARCH Q1 + CONTEXT.md D-09.
- **Strictly sequential 4-wave structure** (1→2→3→4). Plans 02 and 03 share two files (`tree-store.ts`, `PanZoomWrapper.tsx`) but only in non-overlapping regions — sequential execution guarantees clean merges.
- **Modal small-viewport clamp implemented**: `marginTop: topOffset == null ? 0 : clamp(20px, 15vh, ${topOffset}px)`. One-line CSS expression. Resolves RESEARCH Q4.
- **Remove-person undo is local-only in Phase 3**. Server-side `restorePerson` Server Action deferred to Phase 5 as T-03-23. Local zundo-based undo restores the deleted person to the canvas; if the user then edits or drags it, the inverse Server Action will fail and the existing failure UX (red pill + `Couldn't sync history` toast) fires. Demo path (D-01) does NOT exercise this branch. TODO comment planted in source by Plan 03 Task 2 referencing D-03 and Plan 04 T-03-23.
- **No new npm packages**. Toast host, search palette, modal, radial menu, keyboard layer all hand-rolled per UI-SPEC.
- **Plan 04 borderline scope (4 tasks / 10 files) accepted**. Task 4 is a Playwright E2E spec with low implementation weight; the cohesion of Tasks 1–3 (all closure work for the demo path) is preferred over splitting.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-authoring-history/03-01-PLAN.md` — wave 1 baseline; understand `addPerson` RPC contract before Plan 02 consumes it
2. `.planning/phases/03-authoring-history/03-02-PLAN.md` — wave 2; the radial-add user loop
3. `.planning/phases/03-authoring-history/03-03-PLAN.md` — wave 3; zundo wiring (NOTE: shares `tree-store.ts` + `PanZoomWrapper.tsx` with Plan 02 in disjoint regions)
4. `.planning/phases/03-authoring-history/03-04-PLAN.md` — wave 4; demo path closure + E2E gate
5. `.planning/phases/03-authoring-history/03-CONTEXT.md` — 37 locked decisions (D-01..D-37)
6. `.planning/phases/03-authoring-history/03-UI-SPEC.md` — visual + interaction contract (approved)
7. `.planning/phases/03-authoring-history/03-RESEARCH.md` — verified zundo API + Q1..Q5 RESOLVED
8. `.planning/phases/03-authoring-history/03-PATTERNS.md` — concrete code analogs with line ranges per file
9. `CLAUDE.md` — prescriptive stack + project conventions

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Phase boundary that doesn't enable a user loop | Phase 2 shipped a canvas with no way to add people; user said "this doesn't feel like progress." | advisory | Phase 3 SUCCESS GATE is the Playwright demo path E2E (Plan 04 Task 4). Do not mark phase complete on tsc/build alone — the demo path must run green. |
| Concurrent autonomous writes to shared store files | Plans 02 and 03 both modify `tree-store.ts` + `PanZoomWrapper.tsx`. Iteration-1 had them in the same wave. | blocking (resolved structurally) | Wave structure now strictly sequential 1→2→3→4 via `depends_on` frontmatter. Do NOT relax this even if it looks like the plans could parallelize — the regions are disjoint by prose, not by structure. |
| Mocking the database in integration tests | Phase 1 → Phase 2 patterns expect real Supabase + real RLS; mocked tests will pass while RLS regressions ship. | advisory | Test the Server Actions and RPC against a local supabase instance. Plan 01 vitest tests cover pure functions only; integration verification is the Playwright E2E in Plan 04. |

**Severity values:** `blocking` — resuming agent must demonstrate understanding before proceeding. `advisory` — important context.

## Infrastructure State

- **Local supabase:** must be running before Plan 01 Task 2 — the migration runs against it via `npx supabase db push`. If `supabase start` hasn't been run this session, do it first.
- **Working tree:** clean as of commit `f82aa76`.
- **Branch:** `main`, ahead of `origin/main` by 34 commits at handoff time (push planned).
- **Dev server:** not running. Phase 3 is mostly client-side; `npm run dev` will be needed for manual smoke before the Playwright E2E in Plan 04 Task 4.

<context>
The big idea for Phase 3 is shipping the **authoring loop** — the moment when a user clicks `+` on a node, picks a relation, and watches a new person appear with their side panel focused. That single interaction is the product's differentiator (per CONTEXT.md D-01 demo path). Everything else in Phase 3 (undo/redo, toolbar, search, a11y) is in service of making that loop feel effortless.

Wave 1 lays the foundation (utilities + Server Action + RPC + Modal primitive). Wave 2 ships the loop end-to-end (radial menu wired through to optimistic store mutation + Server Action + side-panel auto-focus). Wave 3 wraps the loop in confidence (undo/redo via zundo, toolbar with disabled states, generic toast host). Wave 4 closes (search palette, inline-undo delete, keyboard layer, a11y sweep, E2E).

The demo path Playwright E2E in Plan 04 Task 4 IS the success gate. Don't mark Phase 3 complete on type-check/build alone — the demo path must run green.

One T-03-23 trade-off ships as known debt: server-side restore-person is deferred to Phase 5. The local undo works (zundo restores the deleted node to the canvas); only the eventual server-side reconciliation if the user then edits the restored node fails — and the existing failure UX (red pill + `Couldn't sync history` toast) handles it.
</context>

<next_action>
1. `/clear` to start a fresh context window.
2. Confirm local supabase is running: `supabase status` (or `supabase start`).
3. `/gsd-execute-phase 3` — runs all 4 waves sequentially. Each wave commits per-task; no manual intervention until completion (the supabase db push is automated inside Plan 01 Task 2).
4. After execution: `/gsd-verify-work 3` to walk the demo path.
</next_action>
